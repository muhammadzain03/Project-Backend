# MySQL — Cloud SQL (public IP)
DATABASE_HOST=34.95.8.118
DATABASE_USER=root
DATABASE_PASSWORD=Qwerty$455
DATABASE_NAME=userdb
DATABASE_PORT=3306
DB_POOL_SIZE=20

# Cloud SQL connection name (for Cloud Run --add-cloudsql-instances):
#   seng533-490008:northamerica-northeast1:seng-533-project

# Google Cloud Storage
GCS_BUCKET=userdata003
GOOGLE_APPLICATION_CREDENTIALS=credentials/seng533-490008-98ae5a3775f1.json

# Frontend:
# Leave VITE_API_URL unset in dev — the Vite proxy (vite.config.ts) routes
# /auth and /user to http://127.0.0.1:5000. Only set VITE_API_URL if the
# frontend is deployed and calling the API directly (e.g. a Cloud Run URL).
# VITE_API_URL=
